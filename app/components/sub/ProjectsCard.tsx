import React from "react";

type Props = {
  src: string;
  title: string;
  description: string;
};

const ProjectsCard = ({ src, title, description }: Props) => {
  return <div className="relative ">ProjectsCard</div>;
};

export default ProjectsCard;
